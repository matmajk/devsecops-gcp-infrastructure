---
- name: Bootstrap GKE platform
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    ansible_python_interpreter: "{{ ansible_playbook_python }}"
  roles:
  - role: argocd
    vars:
      gitops_repo_path: "{{ playbook_dir }}/../../../gitops"